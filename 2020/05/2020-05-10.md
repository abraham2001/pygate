baidu的cuda version是9.2
所以需要torch的版本也是9.2
使用这行代码来安装
!pip install -f https://download.pytorch.org/whl/torch_stable.html torch==1.5.0+cu92 torchvision==0.5.0+cu92 


安装后需要这两行代码来验证
print('Make sure cuda is installed:', torch.cuda.is_available())
print('Make sure cudnn is enabled:', torch.backends.cudnn.enabled)

fastai2需要torch>=1.3.0 torchvision>=0.5